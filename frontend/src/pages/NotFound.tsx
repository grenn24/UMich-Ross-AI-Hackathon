const NotFound = () => {
    return (
        <section className="page-shell">
            <div className="panel">
                <h1 className="page-title">Page not found</h1>
                <p className="page-sub">The route does not exist in this demo.</p>
            </div>
        </section>
    );
};

export default NotFound;
